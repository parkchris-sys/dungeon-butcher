# Dungeon Butcher (던전 정육점) — AI 온보딩

> 이 파일은 팀원(기획/아트/개발) 각자의 AI 세션이 가장 먼저 읽는 문서다.
> 프로젝트 상태: **v0.1 초안 — 바이블 미컨펌.** 모든 내용은 변경될 수 있다.

## 게임 한 줄

던전 옆 정육점 주인이 몬스터를 썰어 **고기를 등에 한 조각씩 과장되게 쌓고**, 가게로 돌아와
빠르게 팔아 골드를 불리며, 그 골드로 강화해 더 깊은 던전으로 나아가는
**빠르고 시원한 픽셀아트 액션 타이쿤** (6일 MVP).

## 진실의 원천 (Source of Truth)

| 무엇 | 원본 | 규칙 |
|---|---|---|
| 게임 방향·규칙 | `docs/BIBLE.md` | 여기 없는 건 확정 아님. 첫 방향은 기획이 잡되 이견 제기는 자유 |
| 파트 간 협의·결정 기록 | `docs/DECISIONS.md` | 협의 필요 항목과 결정 기록만. 임의 확정 금지 |
| Phase 계획 | `docs/ROADMAP.md` | 무엇을 어떤 순서로 만들지. Phase 끝 = 플레이 가능 체크포인트 |
| 밸런스 수치 | `docs/BALANCE.md` | 코드/엔진 값은 이 문서의 복사본이다 |
| 에셋 목록·규약·진행 | `docs/ASSET_LIST.md` | 파일명은 여기 규약을 따른다 |
| 파트 간 인계 | `docs/HANDOFF.md` | 토스할 때 한 줄이라도 남긴다 |

PDF·슬라이드·노션 등은 export물일 뿐, 원본이 아니다.

## 작업별 읽기 지도

- **모든 파트 공통**: ① HANDOFF.md 최근 3개 항목 → ② **현재 Phase 기획서 `docs/specs/PHASE1.md`** — "오늘 뭘 하나"는 여기에 있다
- **기획 작업** → BIBLE 전체 + DECISIONS + BALANCE
- **아트 작업** → BIBLE §아트/§플레이어/§적 + ASSET_LIST (팔레트 HEX는 BIBLE에 있음)
- **개발 작업** → BIBLE §루프/§전투/§MVP + BALANCE + HANDOFF 최신 항목
- 회의/안건은 `docs/KICKOFF.md`

## 협업 플로우 (AI가 대신 수행)

이 저장소는 GitHub로 공유된다. 팀원은 git을 몰라도 된다 — **각자의 Claude가 아래 루틴을 대신한다.**

**작업 시작할 때 (항상):**
1. `git pull` 로 최신 동기화
2. `docs/HANDOFF.md` 최근 3개 항목을 읽고 사용자에게 한 줄로 브리핑

**작업 끝낼 때 (항상):**
1. `docs/HANDOFF.md` 맨 위에 한 줄 기록 (`날짜 | 보낸파트→받는파트 | 무엇 | 어디 | 다음 할 일`)
2. `git add -A && git commit && git push`
3. 사용자에게 **슬랙용 요약 한 줄**을 만들어 준다 (사용자가 복사해 슬랙에 올림):
   ```
   [아트→개발] 슬라임 3종 art_incoming에 푸시. 스펙: docs/specs/PHASE1.md §3
   ```

**충돌 예방 — 파트별 홈그라운드:**
- 기획 = `docs/` · 아트 = `art_incoming/` · 개발 = `game/`
- 남의 홈그라운드 파일을 고쳐야 하면 직접 고치지 말고 HANDOFF/슬랙으로 요청
- `HANDOFF.md`는 맨 위에 추가만 (수정·삭제 금지)

**원칙: 진실은 저장소, 슬랙은 알림.** 슬랙에서 결정된 것도 반드시 DECISIONS.md에 기록해야 유효하다.

## 공통 규약

- **`(임의)` 표식 의무**: AI가 초안을 쓰다가 근거 문서 없이 스스로 정한 항목에는 반드시 `(임의)`를 붙인다.
  사람이 검토 후 채택하면 표식을 제거하고, 아니면 고친다. 표식 없는 임의 결정은 규약 위반.
- 미확정 사항을 만나면: 임의로 정하지 말고 `DECISIONS.md`에 항목 추가 → 팀 확인
- 에셋 파일명: 영문 스네이크케이스 (`slime_walk_1.png`, `hub_tile_floor.png`)
- 아트 반입: `art_incoming/`에 넣으면 개발 파트가 처리 (마젠타 #FF00FF 단색 배경, 그림자 없음)
- 게임 프로젝트는 `game/` 아래 (엔진 미정 — DECISIONS 참고)
- 문서는 짧게 유지. 길어지면 쪼갠다
