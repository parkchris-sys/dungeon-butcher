# 파트 인계 로그 (Handoff)

> 파트를 토스할 때 **한 줄이라도** 남긴다. 최신이 위.
> 형식: `날짜 | 보낸파트→받는파트 | 무엇 | 어디에 | 다음 할 일`

---

- 2026-08-07 | 기획→아트 | **아트 스타일 기준 변경: 픽셀아트 → 채색 카툰 아이소메트릭**. 현재 에셋을 나란히 비교한 결과 **닭+오브젝트 11~22는 서로 완전히 일관**한 반면 **플레이어만 톤이 어긋남**(외곽선 약함·어둡고 탁함·거의 정측면 각도). 다수 쪽에 맞추는 게 싸므로 **플레이어를 재제작**하기로. BIBLE §5에 스타일 4요소 명문화(두꺼운 갈색 외곽선 / 밝은 채도·플레이어가 배경보다 밝게 / 통통한 3등신 / 위에서 본 3/4 각도) + **기준 에셋 지정**(닭·오브젝트 11~22). `BIBLE.md` §1·`CLAUDE.md` 게임 한 줄의 '픽셀아트' 문구도 교체. 참고: 개발이 넣은 임시 오브젝트 8종(1~8번)도 새 톤으로 교체 대상 | `docs/BIBLE.md` §1·§5, `docs/DECISIONS.md`, `CLAUDE.md` | 아트: 플레이어 재제작(닭 이미지를 레퍼런스로 첨부 권장) / 개발: 참고만
- 2026-08-07 | 개발→아트/기획 | **닭 좌우 턴 클립 연결 완료** (요청 반영): 몬스터의 **화면 수평 방향이 바뀌는 순간** `chicken_turn_r`/`chicken_turn_l`을 끼워 넣고, 클립의 `next=chicken_walk`로 걷기에 자연 복귀. 구현 요점 3가지 — ① **턴 재생 중에는 좌우 반전을 덮어쓰지 않는다**: 회전을 그림 자체가 담고 있어 매 프레임 `setMirror(faceLeft)`를 계속 걸면 도는 방향이 반대로 보인다(턴 클립 판별 후 스킵, 턴이 끝나면 다시 반전 적용) ② **데드존 0.25타일 (임의)**: 표적이 화면상 거의 수직 방향이면 수평 부호가 떨려 턴이 계속 재생되므로 그 구간은 방향을 유지 ③ **도는 중에는 새 턴으로 끊지 않음** + 스폰 시 초기 방향을 플레이어 쪽으로 미리 맞춰 불필요한 턴 제거. **턴 1종만 반입해도 동작한다** — 없는 쪽은 있는 쪽을 뒤집어 쓰고, 턴이 끝나면 반전 상태가 스스로 방향에 맞춰진다. 검증: 종료 프레임 확인(`turn_r`=오른쪽 보기·`turn_l`=왼쪽 보기, 걷기 기준과 일치) + 3클립 캔버스 267×256 동일(전환 시 크기 안 튐, 표시 91.8×88) | `game/assets/scripts/ingame/{IngameBootstrap,CombatSystem}.ts` | 기획 확인 2건: ⓐ **턴 중에도 계속 이동한다** — 제자리에서 도는 촬영본이라 0.43초(6프레임 fps14) 동안 약 0.6타일 미끄러진다. 멈추고 돌게 할지 / ⓑ 턴 데드존 0.25타일 (임의) / 아트: 턴은 촬영본 2종을 다 쓰고 있음(반전 아님) / ⚠ 실기기 검증은 맵 편집 완료 후 일괄
- 2026-08-07 | 아트(기획 대행)→개발 | **닭 좌우 턴 클립 2종 추가 + 걷기 재정렬**: 턴 영상에서 양방향 턴을 각각 6프레임으로 추출 — **`chicken_turn_r`**(왼→오른쪽 보기로 마침) · **`chicken_turn_l`**(오른→왼쪽 보기로 마침), 둘 다 정면을 거쳐 도는 동작. loop=false, **next=`chicken_walk`** 로 걷기에 자연 연결. **걷기·턴 3클립을 같은 배율·같은 캔버스(267×256)로 재생성** — 클립 전환 시 크기·위치가 튀지 않게(애니메이터가 첫 프레임 비율로 표시 크기를 잡으므로 캔버스가 다르면 튄다). 전부 오른쪽 보기 기준. ⚠ **개발 작업 필요**: 현재 몬스터는 `playMonsterAnim(kind,'walk')`로 walk만 재생 — 방향이 바뀌는 순간 턴 클립을 끼워 넣는 처리가 없다. 참고: 미러링만 쓰면 턴 1종으로도 충분하지만(한쪽의 반전=반대쪽), 실제 촬영본 2종을 다 넣어뒀으니 필요한 쪽만 써도 됨 | `resources/chars/chicken_{walk,turn_r,turn_l}_1~6.png`·`resources/anim/animdata.json` | 개발: 방향 전환 시 턴 클립 재생 연결 / 기획: 턴 fps 14 체감 확인
- 2026-08-07 | 개발→아트/기획 | **8방향 + 클립 7종 + 전투 규칙 3건 구현 완료** (실기기 검증은 맵 편집 후로 미룸): ① **주인공 8방향** — 조이스틱 입력을 **화면 기준 균등 8등분**(±22.5°)으로 스냅, 방향 토큰은 규약대로 `n·ne·e·se·s·sw·w·nw`. **폴백 4단계**로 아트를 순차 반입 가능: 그 방향 클립 → **좌우 반전 짝**(`ne`↔`nw`, `e`↔`w`, `se`↔`sw`) → 좌/우(e·w) 클립 → 무방향 클립. **지금 좌/우 2방향 예제만으로 8방향이 정상 동작**하고, 그리는 방향부터 하나씩 살아난다 (한 방향만 넣어도 됨). ② **클립 7종 상태 폴백** — 만재면 `idle_heavy`/`walk_heavy` 우선(없으면 `idle`/`walk`), 공격은 이동 중 `attack_walk`·정지 중 `attack_idle` 우선(없으면 `attack`). **상태 폴백보다 방향 폴백을 먼저** 시도한다(만재·이동공격 같은 상태 정보가 방향 정확도보다 체감이 크므로). ③ **바라보는 방향의 적만 공격** — 표적 선택에 시야 원뿔 판정 추가, **07-31 자동 회전 구현은 되돌림**. ④ **만재 시 공격 불가** — 만재면 공격 자체가 안 나가고 **땀 표시**(물방울 2개 플레이스홀더 — **중량 초과 클립이 반입되면 자동으로 꺼진다**). ⑤ 예제 클립·프레임을 새 토큰으로 정리: `player_{상태}_{left|right}_*` → **`_e`/`_w`** (파일 20장 + animdata.json 키) | `game/assets/scripts/ingame/{Facing8,IngameBootstrap,CombatSystem}.ts`·`resources/chars/`·`resources/anim/animdata.json` | **아트: 방향은 화면 기준이다** — N=화면 위, E=화면 오른쪽 (아이소라 월드 방위와 화면 각도가 어긋나므로 코드는 화면 기준을 채택. 그리는 그림·조이스틱 입력과 일치) / **기획 확인 3건**: ⓐ **이동도 8방향으로 제한할지** — 현재는 **이동 자유·스프라이트만 8방향 스냅**. 아날로그 조이스틱에서 이동까지 잠그면 걸리는 느낌이라 개발 의견은 현행 유지 ⓑ **시야 원뿔 반각 60° (임의)** — 45°로 하면 대각 방향에서 이웃 타일이 정확히 경계에 걸려 판정이 들쭉날쭉함. BALANCE에 항목을 만들어 주면 이관 ⓒ **정지 중에는 방향 전환 수단이 없음**(방향은 이동으로만 바뀜) → 등진 적을 치려면 그쪽으로 걸어야 함. 의도한 조작인지 확인
- 2026-08-07 | 아트(기획 대행)→개발 | **닭 아트 반입 — 걷기 6프레임 + 정지 이미지**: 생성 영상(8초)에서 걷기 사이클을 프레임 유사도로 검출(15프레임=1.25초)하고 **6장으로 압축** → `chars/chicken_walk_1~6.png`(232×256, 공통 캔버스). `anim/animdata.json`의 `chicken_walk`를 **4프레임 fps8 → 6프레임 fps10**으로 갱신. 정지 이미지는 `maps/units/7_chicken.png`(212×256)로 추가 — 에디터에서 몬스터 배치 시 외형 지정용. ⚠ **원본 영상은 왼쪽을 보고 걷지만, 저장은 오른쪽 보기로 좌우 반전**해서 넣음 — 엔진이 `setMirror(faceLeft)`로 왼쪽 이동 시 자동 반전하므로 기본 클립은 오른쪽이어야 맞다. 참고: 몬스터 기본 외형은 여전히 초록 플레이스홀더 박스이고 애니메이터가 클립을 씌우는 구조라, 정지 이미지는 에디터 배치용으로만 쓰인다 | `resources/chars/`·`resources/maps/units/`·`resources/anim/animdata.json`, `tools/make_meta.js` | 개발: 실기기에서 6프레임 재생·크기(88px) 확인 / 기획: fps 10 체감 확인
- 2026-08-07 | 기획→개발/아트 | **플레이어 8방향 상향 + 애니메이션 클립 7종 확정**: 오늘 오전 4방향 결정을 **8방향**으로 상향(몬스터는 좌우 유지). **좌우 반전 쓰면 제작은 5방향**(n·ne·e·se·s → w/nw/sw 자동 반전). 클립 7종 = 필수 3(idle·walk·attack) + 중량초과 2(idle_heavy·walk_heavy) + 공격분화 2(attack_idle·attack_walk). **중량초과 공격 클립 없음**(만재 시 공격 불가 결정과 일관). 파일명 `chars/{클립키}_{방향}_{n}.png`. 총 35클립(8방향 전부면 56) | `docs/BIBLE.md` §6-a·§6-b, `docs/DECISIONS.md`, `docs/SYSTEM_CARDS.md` | 개발: 현재 좌/우 2방향 전용 로직(`playDirectional`)을 8방향으로 확장 필요 / 아트: 필수 3종부터 → 중량초과 → 공격분화 순
- 2026-08-07 | 아트(기획 대행)→디자이너 | **던전1 맵 꾸미기 반영**: `2_dungeon_floor`를 윗면 정확히 1024×512로 재생성(구역 32×32 × floorScale 4 = 4096×2048 정합). **마을 구역(id 99) floorImg 3→1** — 새로 넣은 돌바닥을 실제로 쓰게 변경(원복하려면 3으로). 던전 d1에 **장식 26개 배치**(이동불가 14: 바위·울타리·건초·여물통 / 통과가능 12: 풀·꽃·둥지·짚) — 중앙 전투 공간과 게이트 입구 통로는 비움. 빌드로 데이터 반영 확인 | `resources/maps/mapdata.json` | ⚠디자이너: 에디터에서 **import 후 작업**할 것 (에디터에서 그냥 재내보내면 이 배치가 사라짐) / imgScale·imgOffY는 1차값이라 눈으로 보고 조정 필요
- 2026-08-07 | 아트(기획 대행)→디자이너 | **던전 1단계 바닥 교체 + 배치 오브젝트 12종 반입**: `2_dungeon_floor` 를 새 풀·흙 다이아몬드로 교체(윗면 2:1 정중앙 정렬, uuid 유지). 오브젝트 **11~22번** 신규 — 바위 4종(rocks_large/rocks_medium/rock_small/rocks_cluster), 건초 3종(haybale/straw_pile/nest_eggs), 울타리(fence_wood)·여물통(trough)·식물 3종(bush/grass_tuft/flowers). 마젠타 시트에서 낱개로 분리, 그림자는 중성 검정 반투명으로 복원 | `resources/maps/floors/`·`resources/maps/objs/`, `tools/slice_sheet.js` | 디자이너: 에디터에서 던전 구역에 오브젝트 배치(img=11~22) 후 재내보내기
- 2026-08-07 | 기획→개발 | **판매 골드 = 즉시 반영, 회수 스텝 제거 (재확정)**: 조리→운반→판매까지는 그대로 두고, **판매 시점에 즉시 `addGold`**. 현재 구현된 **`정산대(checkout) → 회수위치(money-pickup)` 2스텝은 폐기** — 떨어진 돈을 주우러 가는 동작 없음. 원칙: **고기는 운반 대상, 돈은 아님.** ※2026-07-24 결정(§9-a 4번)이 이미 '즉시 자동'이었으나 구현이 회수 체인으로 되어 있어 재확인·리워크 요청. ⚠확인 요청: 기획이 "상단 HUD에 바로 들어가도록"이라 표현했는데, §10-a는 **상시 골드 카운터 없음(획득 시 +N 팝업만)**으로 확정돼 있음 — 상시 표시를 원하는지 팝업 유지인지 회신 필요 | `docs/BIBLE.md` §9-a, `docs/DECISIONS.md`, `docs/SYSTEM_CARDS.md`(카드 5) | 개발: 회수 2스텝 제거 + 판매 즉시 골드
- 2026-08-07 | 기획→개발/아트 | **전투·방향 규칙 3건 확정**: ① **플레이어 4방향(동서남북)** 이동·스프라이트(idle·walk·attack 각 4방향), **몬스터는 좌우 2방향 유지** ② **공격은 바라보는 방향의 적만** — 등진 적에게 자동 회전 금지 (**개발 07-31 문의 답변**: 표적 자동 회전 구현은 채택 안 함) ③ **등짐 만재 시 공격 불가** — 줍지도 싸우지도 못해 귀환 강제, 상태는 땀 흘리기 등으로 표현(연출 추후). ⚠확인 필요: 북쪽(뒤) 볼 때 등짐 스택이 캐릭터를 가리는 문제 — 아트·개발 협의 | `docs/BIBLE.md` §3·§6-a·§8, `docs/DECISIONS.md`, `docs/SYSTEM_CARDS.md` | 개발: 4방향 애니 지원 + 공격 방향 조건 + 만재 공격 잠금 / 아트: 플레이어 4방향 세트(물량 2배), 땀 표현
- 2026-07-31 | 개발→기획/아트 | **공격 애니메이션 방향 = 표적 방향으로 수정** (실기기 검증 ✅): 기존에는 공격 클립 방향을 **이동 방향(`facing`)** 으로 골랐기 때문에 **왼쪽으로 걸으며 오른쪽 적을 치면 왼쪽 공격이 재생**되고 칼질 이펙트만 표적 쪽에 뜨는 어긋남이 있었음. 이제 `playAttackAnim(faceLeft)` 로 **표적의 화면 x 방향**을 넘겨 그쪽을 보고 휘두르고, 공격 후에도 표적을 보고 있게 `facing`까지 갱신(표적이 화면상 정위/정하면 현재 방향 유지). 공격 중에는 방향이 고정돼 중간에 뒤집히지 않음. **같이 잡은 버그 1건**: 공격 중 매 프레임 `setMirror(playerAttackFacing==='left')` 를 다시 걸어서, **방향별 클립(`player_attack_left`)이 한 번 더 뒤집혀 반대로 보이던** 문제 — 방향별 클립은 `playDirectional`이 이미 반전 없이 재생하므로 공격 중 재반전을 제거. 검증: ① 좌향 대기 중 우측 적 공격 → 우향으로 돌아 휘두르고 그대로 우향 유지 ② 우측으로 이동 중 좌측 적 공격 → 좌향으로 휘두름(뒤집힘 없음) | `game/assets/scripts/ingame/{IngameBootstrap,CombatSystem}.ts` | 기획: **공격 중 이동 방향과 바라보는 방향이 달라지는 연출을 허용할지** 확인 (현재는 표적 우선 = 액션게임 통례, 임의) / 아트: attack 클립은 표적 쪽을 향해 휘두르는 그림이면 됨
- 2026-07-31 | 개발→아트/기획 | **애니메이션 실기기 최종 검증 완료** ✅ (32비트 폰): `로비 → 마을 → 게이트 해금 → 던전 → 사냥(공격·타격·고기 스택)` 전 구간에서 방향별 클립(플레이어 좌/우 idle·walk·attack)과 닭 walk 정상 재생, JS 에러 0건. **검증 중 잡은 버그 2건**: ① **닭이 초록색으로 물듦** — 플레이스홀더 박스 틴트(`#3B7A54`)가 남아 실제 그림에 곱해짐 ② **닭 비율·크기 어긋남 + 눈 박스가 그림 위에 겹침** — 박스 규격(64×46)을 그대로 물려받음. 해결: 애니메이터에 **`setFitHeight()`** 추가 — 클립이 바뀔 때 **첫 프레임의 원본 비율**로 표시 크기를 다시 맞추고 **틴트를 흰색으로 리셋**(플레이어=높이 128, 몬스터=88 임의). 눈 스프라이트는 애니메이터가 붙으면 아예 만들지 않음. ⚠ 즉 **아트 프레임의 캔버스 비율이 그대로 화면 비율** — 패딩을 넣어도 안전하지만 프레임마다 캔버스 크기가 다르면 재생 중 크기가 튄다(클립 내 캔버스 통일 필수) | `game/assets/scripts/ingame/{SpriteAnimator,IngameBootstrap,CombatSystem}.ts` | 아트: **한 클립의 프레임은 캔버스 크기를 반드시 동일하게**, 발이 캔버스 하단에 닿게 그리면 접지 위치가 맞음 / 기획: 몬스터 표시 높이 88px 확인 (임의) · 좌우 반전 허용 여부 / 남은 후속: 몬스터가 던전 한쪽 구석에 뭉치는 현상
- 2026-07-24 | 개발→아트 | **애니메이션 좌우 방향 처리 — 아트 물량을 데이터로 선택**: 플레이어는 기존에 좌/우 정적 스프라이트를 따로 썼으므로 애니메이션도 방향이 필요. **두 방식을 모두 지원**하게 구현: ① `{종류}_{상태}_left` / `_right` 클립이 **있으면 그 클립을 그대로 사용**(양방향을 따로 그린 경우 — 칼 든 손처럼 비대칭을 살릴 수 있음) ② **없으면** `{종류}_{상태}` 한쪽 클립을 **좌우 반전**해서 사용(**물량 절반**). 즉 아트가 나중에 어느 쪽이든 선택 가능하고 코드 변경은 불필요. 공격 중에는 시작 시점 방향을 고정(중간에 뒤집히지 않음), 애니메이션이 돌 때는 기존 정적 좌/우 교체를 끔(덮어쓰기 충돌 방지). **몬스터도 동일 규칙** — 화면 수평 이동 방향으로 자동 반전 | `game/assets/scripts/ingame/{SpriteAnimator,IngameBootstrap,CombatSystem}.ts` | 아트: **한쪽만 그려도 동작함**(권장 — 물량 절반). 양방향을 따로 그리고 싶으면 파일명을 `player_walk_left_1.png`처럼 방향까지 넣고 클립 키를 `player_walk_left`로 만들면 됨 / 기획: 좌우 반전 허용 여부만 확인해주면 아트 물량이 확정됨
- 2026-07-24 | 개발→아트/기획 | **캐릭터 스프라이트 애니메이션 + 애니메이션 편집 씬 신설**: 방식은 **프레임(스프라이트) 애니메이션** — 픽셀아트·저사양 타깃에 맞고 스켈레탈(Spine 등)은 제외. 클립 데이터는 **자체 JSON**(`resources/anim/animdata.json`)으로 관리(맵 데이터와 동일한 흐름: 편집 씬 → JSON → 게임은 JSON만 읽음). cc.Animation을 쓰지 않은 이유: 몬스터·손님이 런타임 생성+풀 재사용이라 클립 에셋·프리팹 방식과 궁합이 나쁨. **프레임별 "기교" 편집 지원**: `hold`(노출 시간 배수 — 짧게 스치거나 길게), `offX/offY`(반동·통통 튀기), `scaleX/scaleY`(스쿼시&스트레치), **`event` 마커**(`hit` 프레임에 실제 데미지가 들어가도록 타격 타이밍 일치 — 이벤트 없으면 안전 타임아웃으로 타격 유실 방지). **애니메이션 편집 씬**(`assets/animedit/animedit.scene`, 빌드 제외): 클립 노드=클립 1개(키·fps·loop·next), 자식 노드=프레임(자식 순서=재생 순서), **실시간 미리보기 재생**, `autoFillFrames`로 이미지 파일 스캔해 프레임 자동 생성, export/import JSON 왕복. **아트 미반입 시 기존 정적 이미지로 폴백**(애니메이션 비용 0)이라 지금 바로 넣어도 안전 | `game/assets/scripts/ingame/{AnimData,SpriteAnimator}.ts`·`animedit/*`·`assets/animedit/animedit.scene`·`resources/anim/animdata.json` | **아트: 프레임 파일 `resources/chars/{종류}_{상태}_{n}.png`** (규약대로 `_1.png~_N.png`), 128×128, **8~12fps 기준 idle 2~4·walk 4~6·attack 3~4장**, **좌우 반전 사용 권장(한쪽만 그리면 물량 절반)**. 1차는 **플레이어 walk + 닭 walk만** 권장(체감 8할) / 기획: fps·프레임 수·좌우 반전 여부 확인 (전부 임의) / 예제 클립 4개(player idle/walk/attack, chicken_walk)를 넣어뒀으니 편집 씬에서 importJson으로 확인 가능
- 2026-07-24 | 개발→전체 | **팝업 2종 실기기 검증 완료 (게이트·강화)** ✅: 32비트 폰에서 `로비 → 마을 → 게이트 팝업 → 해금 → 문 열림 → 던전 진입`, `강화 발판 → 팝업 → 구매(골드 차감·수치 즉시 갱신)` 전 구간 동작 확인. **검증 중 잡은 버그 3건**: ① **팝업 버튼이 안 눌림** — 버튼 터치가 조이스틱으로도 들어가 플레이어가 밀리며 팝업이 파괴돼 TOUCH_END 유실 → **팝업 패널 위 터치만 조이스틱 제외**(딤 영역은 이동 가능해야 팝업을 닫을 수 있으므로 전면 차단은 교착) ② **해금해도 던전문이 안 열림** — 게이트에 문 오브젝트가 `objectLink1`으로 연결되지 않아 그 오브젝트가 계속 차단 → **미연결 시 시작 경고 추가**(넣어야 할 objectId까지 안내) ③ 큰 볼드 폰트 글리프 겹침 → `cacheMode=BITMAP`. 시스템 카드 7(강화)·8(게이트) 갱신 | `game/assets/scripts/ingame/{IngameBootstrap,TriggerSystem}.ts`·`docs/SYSTEM_CARDS.md` | **디자이너: 에디터에서 게이트(`trigger-1`)의 `objectLink1`에 `gate1` 설정 후 재내보내기 필요** — 개발이 mapdata에 임시로 넣어 검증했으나 재내보내기 시 사라짐. 앞으로 게이트마다 문 오브젝트를 연결할 것 / 기획: 강화 비용 곡선·구역 해금 비용 확정 (현재 전부 임의)
- 2026-07-24 | 개발→기획/아트 | **강화 3종 발판 + 강화 팝업 구현** (BIBLE §7-a·§7-b·§10-b): `upgrade` 트리거(발판) 신설 — Inspector 드롭다운으로 **공격력/이동속도/운반** 선택. 발판 위에서 **팝업 자동 열림·이탈 시 닫힘·닫기 버튼 없음**, **표시 4종**(현재 스탯 · 강화 후+증가분 초록 강조 · 보유 골드 · 필요 골드), **1회 구매 / 10회 구매**(10회는 자금 부족 시 **가능한 만큼만** — §10-b 결정), 구매해도 팝업은 닫히지 않고 수치만 즉시 갱신. **효과가 실제로 반영됨**: 공격력→타격 데미지(닭 2타→1타 브레이크포인트), 이동속도→이속(무게 페널티와 합산), 운반→등짐 한계(HUD·픽업 판정 모두 연동). ⚠ **비용 곡선이 BALANCE TBD라 임의값 사용**: `TriggerSystem.ts UPGRADE_SPEC` = 비용 `50 + 10×레벨`, 증가폭 공격력 +1·운반 +2(BALANCE 스케치 값)·**이동속도 +20px/s(임의 — BALANCE에 항목 없음, 무게 페널티 1개분 상쇄 의도)`. 확정 시 이 표 한 곳만 수정하면 됨. ⚠ 강화 레벨은 **세션 내 유지**(세이브 더미) | `game/assets/scripts/ingame/{TriggerSystem,CombatSystem,IngameBootstrap,MapData}.ts`·`mapedit/{MapTrigger,MapEditRoot}.ts`·`docs/SYSTEM_CARDS.md`(카드 7) | **기획: 강화 비용 곡선 + 이동속도 증가폭 확정 필요**(현재 전부 임의) · C9(오브젝트 외형 변화) / 디자이너: 마을에 강화 발판 3개 배치 + 종류 지정, 무기손질대·신발정비대·운반구공방 오브젝트를 발판에 겹쳐 두기 / 아트: 오브젝트 3종·팝업 프레임
- 2026-07-24 | 개발→디자인/기획 | **구역 해금 게이트 + 해금 팝업 구현** (BIBLE §7-c·§10-b): 맵 에디터에 **`gate` 트리거** 신설 — Inspector `targetRegionId`(목적지 지역 ID, 팝업에 그 지역 라벨 표시)·**`unlockCost`**(해금 비용, **0이면 무료 통과 배지** = 최초 마을→사냥지대 튜토리얼). 동작: 잠긴 게이트 타일은 **통과 불가(문 역할)**, **문 앞에 서면 팝업 자동 열림·벗어나면 자동 닫힘·닫기 버튼 없음**(§10-b 공통 규칙), 목적지 이름·해금 비용·보유 골드 표시(**부족하면 빨강 + 버튼 비활성**), 해금 시 골드 차감 후 개방 배너 → 그대로 통과. ⚠ 규격의 "문 앞 발판"은 **인접 판정으로 대체**(게이트 타일이 통과 불가라 그 위에 설 수 없으므로 — 임의). ⚠ 해금 상태는 **세션 내 유지**(세이브 더미라 영구 저장 없음). export/import 왕복 보존 | `game/assets/scripts/ingame/{TriggerSystem,IngameBootstrap,MapData}.ts`·`mapedit/{MapTrigger,MapEditRoot}.ts`·`docs/SYSTEM_CARDS.md`(카드 8) | 디자이너: 맵 경계에 게이트 트리거 배치(문 폭만큼) + targetRegionId·unlockCost 지정, 첫 경계는 unlockCost=0 / 기획: **구역별 해금 비용 확정**(현재 에디터 입력값 그대로 사용) / 다음 개발: 강화 팝업은 **비용 곡선(BALANCE TBD)** 확정 후 착수 가능
- 2026-07-24 | 개발→아트/기획 | **인트로+로비 개발분 완료 (실기기 검증)** ✅: **`assets/scenes/lobby.scene` 신설 = 새 시작 씬**. 흐름 `앱 실행 → [최초 1회 인트로 3컷] → 로비 → (버튼) → 마을` 전 구간 32비트 폰에서 동작 확인. 구현: 인트로 3컷 자동 재생(컷당 3s, 임의)·**건너뛰기 버튼**·**최초 1회 플래그**(localStorage `db_intro_seen` — §5대로 이것이 유일한 저장 항목), 로비 버튼 4종(처음부터하기·이어하기=둘 다 마을 진입/설정 팝업 자리/**종료는 웹빌드에서 자동 숨김**), 로고·버전 표기. **아트는 플레이스홀더** — `resources/lobby/bg.png`·`intro_1.png`~`intro_3.png`를 넣으면 코드 수정 없이 자동 적용(없으면 색판+안내문). ⚠ 실기기 이슈 1건 수정: **큰 볼드 폰트(100px+)가 글리프 겹침으로 깨짐** → 로고 80px + `cacheMode=BITMAP`, 인게임 존 배너(72px)도 동일 적용. ⚠ **빌드 시작 씬을 lobby로 바꿨음**(profiles는 git 미추적이라 다른 PC에서 재설정 필요) | `game/assets/scenes/lobby.scene`·`scripts/lobby/LobbyBootstrap.ts`·`docs/SYSTEM_CARDS.md`(카드 9) | 아트: 로비 배경(세로 구도)·인트로 3컷·로고 → `resources/lobby/` / 기획: 인트로 제작 방식 확정·자막 문구(현재 임의 3줄)·설정 항목
- 2026-07-24 | 개발→전체 | **안드로이드 실기기 검증 완료 (32비트 저사양)** ✅: **moto g play 2023 / armeabi-v7a 전용(64비트 미지원) / PowerVR GE8320** 에서 정상 동작 확인 — **56~59 FPS 유지**(60 상한), 통짜 바닥 이미지·오브젝트·바닥 데칼 정상 렌더(**UI 버퍼 9004 재발 없음**), 화면 밖 컬링 정상, HUD 규격대로(마을=HP바 숨김 / 사냥지대=머리 위 HP바 표시, 상시 코너 HUD 없음), 등짐 스택·몬스터 웨이브·조이스틱 모두 정상. ⚠ **빌드 함정 발견·수정**: CLI 빌드가 쓰는 `profiles/v2/packages/builder.json` 의 `common` 섹션에 **startScene이 없고 scenes가 옛 경로**(`assets/resources/ingame/…`)여서, 빌드 산출물의 `launchScene`이 **빌드 제외된 mapedit.scene** 으로 잡혀 **스플래시에서 영구 대기**(로그·에러 없음 — 원인 파악 어려움). 해결: CLI에 `startScene=1a741871-2ba0-450a-949c-9a1cf73ccb5f`(=`assets/scenes/ingame.scene`) 명시 + common 설정 정정. **profiles/는 git 미추적(로컬)이라 다른 PC에서 재발 가능** — 빌드 패널에서 시작 씬이 ingame인지 확인 필요 | `game/build/android/`·`profiles/v2/packages/builder.json`(로컬) | 개발 다음: 로비/인트로 씬 / 관찰된 후속 과제: 몬스터가 던전 한쪽 구석에 뭉치는 현상(스폰 위치·소속 던전 제약 상호작용 의심) 확인 필요
- 2026-07-24 | 개발→전체 | **정육식당 전체 체인 실플레이 검증 완료** ✅: `리소스이동A(생고기) → 고기굽기 → 리소스이동B(회수) → [플레이어 운반] → 리소스이동C(요리) → 판매대 → 구매위치 → 정산대 --link2--> 리소스이동D(돈 회수)` 전 구간 정상 동작 확인. **타이쿤 루프(사냥→운반→조리→운반→판매→골드)가 처음으로 끝까지 연결됨.** 시스템 카드 4(조리)·5(판매) 상태 🔧→✅ | `docs/SYSTEM_CARDS.md` | 개발 다음: 안드로이드 빌드 실기기 검증(바닥 이미지·컬링·HUD·카메라 클램프) → 로비/인트로 씬 / 기획: 부록 미결 7건(특히 손님 1명↔2명 상충, 강화 비용 곡선)
- 2026-07-24 | 개발→전체 | **트리거 체인 조립 수정 (검증 실패 대응)**: ① **모든 트리거에 `triggerLink2` 추가** (2번 연결, 선택) — 내보내기 시 1번을 비우고 2번만 써도 인덱스 자리 보존. ② **정산대(checkout)는 회수한 돈을 link2로 이송** (비우면 종전처럼 자기 자리에 쌓임 → 회수위치가 가져감). ③ **체인 단절 버그 수정** — `고기굽기`가 출력 대상을 `판매대` 타입으로만 인정해 `고기굽기 → 리소스이동B`로 연결하면 대상이 null이 되어 거기서 체인이 끊겼음. **`player-resource`를 범용 리소스 보관소로 인정**하도록 완화(전 트리거 공통) → 조립식 플로우 성립. 검증된 조립 경로: `리소스이동A(생고기) → 고기굽기 → 리소스이동B(회수) → 리소스이동C(요리) → 판매대 → 구매위치 → 정산대 --link2--> 리소스이동D(돈 회수)` | `game/assets/scripts/ingame/TriggerSystem.ts`·`mapedit/{MapTrigger,MapEditRoot}.ts`·`docs/SYSTEM_CARDS.md`(카드 4·5) | 디자이너: 위 경로로 재배치·재내보내기 후 재검증 / 개발: 실플레이 검증 계속
- 2026-07-24 | 기획→전체 | **시스템 카드 신설** (`docs/SYSTEM_CARDS.md`) — 타 팀(BingoDuel) 문서정리 배포판에서 차용. 시스템 11개(사냥·드랍/픽업·운반·조리·판매·손님·강화·구역해금·인트로/로비·맵·비서NPC)를 **카드 한 장씩**으로 정리, 카드마다 동작·상태·수치 위치·**직군별 할 일**이 붙어 있음. **수치는 복제하지 않고 정본 위치만 가리킴**(복제하면 어긋나므로). CLAUDE.md 읽기 지도가 여기를 먼저 가리키도록 수정 + "시스템 변경 시 카드도 같은 커밋에 갱신" 규약 추가. 부록에 **지금 기획이 정해야 할 7건**을 카드에서 추출해둠 | `docs/SYSTEM_CARDS.md`, `CLAUDE.md` | 전원: 어느 문서 볼지 모르겠으면 카드부터 / 기획: 부록 7건 처리
- 2026-07-24 | 개발→전체 | **밸런스 v0.3 반영 + HUD 최소화 + 플레이어리소스이동 트리거 신설**: ① **BALANCE v0.3 코드 동기화** — 스폰주기 1.6s·동시최대 22·피격무적 0.5s·몬스터속도 1.4, 몬스터 키를 **slime→chicken(닭)** 으로 리네임(에디터 기본 kind도 chicken). ② **HUD 최소화(BIBLE §10-a 준수)** — 상시 코너 HUD(고기 카운터·골드 라벨) 제거, **HP바를 캐릭터 머리 위 플로팅 + 사냥지대에서만 표시**(마을 숨김), 골드는 획득 시 **"+N" 팝업**. ③ **플레이어리소스이동 트리거(player-resource) 신설** — 기존 트리거 6종은 **제거하지 않고 유지**, 이 타입을 추가해 조립식으로 새 플로우 구성 가능: 플레이어가 영역에 서 있을 때 **연결 트리거 있으면** 보유 리소스를 그 트리거로 순차 이송, **연결 없으면** 트리거에 쌓인 리소스를 플레이어가 회수. Inspector `resource`(생고기/요리/돈) 로 대상 지정. 등짐이 **생고기·요리 혼재**를 지원(조각 색으로 구분, 한계 10 공용). 돈은 등에 지지 않고 회수 시 즉시 골드. 예: `리소스이동A → 고기굽기 → 리소스이동B(회수) → 리소스이동C → 판매대 → 구매위치 → 정산대 → 리소스이동D(회수)` | `game/assets/scripts/ingame/{CombatSystem,TriggerSystem,IngameBootstrap}.ts`·`mapedit/{MapTrigger,MapEditRoot}.ts` | 디자이너: 새 플로우를 리소스이동 트리거로 조립(연결 비우면 회수 지점) / 기획: **손님 타일당 1명 vs 2명 확정 필요** — 현재 코드는 1명(요청 반영), 문서(DECISIONS·§9-a)는 2명이라 상충 / 참고: 조리·판매 리워크는 기존 트리거 제거 없이 신규 트리거 조합으로 해결하는 방향
- 2026-07-24 | 개발→전체 | **NPC 스폰 트리거 + 손님 NPC 대개편**: ① **스폰 트리거**(npc-spawn) 신설 — 조건 충족 시 손님 스폰(기획 규칙 정의 전 **10초에 1명**, 임의). 스폰마다 **spawnId 1씩 증가**해 부여(트리거 간 순서 판단용). 스폰 위치 두 슬롯이 다 차면 그 주기 건너뜀. 스폰 트리거 Inspector `npcImg`로 외형 지정. ② **손님은 이제 스폰 트리거로만 생성**(정적 배치 폐기) — CustomerSystem 풀 재사용. ③ **타일당 2명**(2:1 타일 좌/우 반씩, 슬롯 0/1). ④ **참을성/기분/성향**(전부 임의): 성향 급함·느긋함·시간무한 = 참을성 10·30·9999, 10초마다 1↓, 0이면 기분 1단계↓ 후 리셋. 기분 화남<지루함<보통<행복함, 머리 위 이모티콘(maps/moods 4종). 요리 받으면 행복. **화남에서 소진 시 즉시 포기 → 퇴장 타일로 나감**(퇴장 인접 없으면 사라짐 — 임의). ⑤ **구매위치**: 2명이면 **spawnId 작은(먼저 온) 손님 우선** 응대 | `game/assets/scripts/ingame/{TriggerSystem,CustomerSystem,IngameBootstrap}.ts`·`mapedit/{MapTrigger,MapEditRoot}.ts`·`resources/maps/moods/` | 디자이너: 대기열 시작에 **스폰 트리거** 배치(기존 customer NPC 정적 배치 대신), npcImg 지정 / 기획: 스폰 규칙·참을성/기분/성향 수치·포기 처리 정의 필요 (전부 임의)
- 2026-07-24 | 개발→디자인 | **오브젝트 리전 종속 + 이미지 크기 분리**: ① MapObject에 **regionId** — 값 입력 시 그 오브젝트 노드가 해당 리전의 `objects` 자식으로 편입되어 **리전과 함께 이동**(props와 동일 구조, 0=전역). 절대 위치 보존하며 자동 편입, export는 절대좌표로 변환·왕복 보존. ② **이미지 크기를 타일 크기(tileW/H)와 분리** — 발자국(점유)만 타일 단위, 외형 이미지는 `imgScale`(배율)·`imgOffX/Y`(px offset)로 독립 조절(앵커는 발자국 하단 꼭짓점 유지). 타일 크기를 바꿔도 이미지 크기는 안 변함 | `game/assets/scripts/mapedit/MapObject.ts`·`MapEditRoot.ts`·`ingame/{IngameBootstrap,MapData}.ts` | 디자이너: 오브젝트에 종속 리전 ID 지정하면 리전 이동 시 함께 움직임 / 이미지가 작으면 imgScale 키우기 / 기획: imgScale 기본 1(임의)
- 2026-07-24 | 개발→전체 | **바닥을 통짜 이미지로 전환 (타일별 이미지 폐지)**: 타일 디자인 시간이 너무 커서, 바닥을 타일마다 지정하지 않고 **구역(TileRegion) 하나에 큰 바닥 그림 1장**을 지정하는 방식으로 변경. TileRegion Inspector에 `floorImg`(resources/maps/floors/{ID}_{이름}.png)·`floorScale`(배율)·`floorOffX/Y`(px offset) 추가 — 에디터에서 크기·위치를 눈으로 맞추면 게임이 동일하게 렌더(같은 아이소 투영). **타일별 img 편집 제거**, 타일은 이제 **속성(attr)·존(zone)만** 편집(색으로만 표시). 게임은 바닥 이미지를 그리며 **화면 밖 이미지는 안 그림(컬링)**. 바닥 없는 곳은 TileView가 존 색 베이스만 깔아줌. 예제 바닥 3종(village/dungeon/grass, 1024×512 아이소 다이아몬드) | `game/assets/scripts/{ingame,mapedit}/`·`resources/maps/floors/` | 디자이너: 각 구역에 통짜 바닥 이미지 제작·지정(floorScale/off로 정렬) 후 재내보내기 / 참고: 기존 resources/maps/tiles(타일별 50종)는 이제 미사용 — 추후 정리 가능 / 기획: floorScale 기본값·offset은 임의(에디터에서 조절)
- 2026-07-24 | 기획→전체 | **인트로+로비 기획서 신설** (`specs/INTRO_LOBBY.md` v0.1): 트럭 환생 인트로 → 로비 → 마을 흐름 정의. **인트로는 최초 1회만**(세이브 유무로 판정), 로비는 **인게임과 분리된 별도 화면** + 버튼 4개(처음부터하기·이어하기·설정·종료). **세이브는 더미** — 로비는 화면 구성이 목적이라 처음부터하기/이어하기 둘 다 마을 진입이면 충분, 인트로 최초1회 플래그만 있으면 됨(저장 항목 설계 불필요). 종료 버튼은 **웹빌드에서 숨김** 필요. 인트로 제작 방식(스틸컷3장 권장/영상/엔진연출)은 미결 | `docs/specs/INTRO_LOBBY.md`, `docs/BIBLE.md` §4, `docs/DECISIONS.md` | 기획: 제작 방식·자막 확정 / 아트: 로비 배경(⚠참고이미지는 가로였음, 세로 재구성 필요)·인트로 3컷 / 개발: 로비 씬·세이브 구조
- 2026-07-24 | 기획→개발 | **밸런스 v0.3 — 첫 맵 난이도 +30%** + **팝업 UI 규격 문서화**. 밸런스 4곳만 변경(`CombatSystem.ts BAL` 갱신 필요): **스폰주기 2.0→1.6s** · **동시최대 18→22** · **피격무적 0.6→0.5s** · **닭 속도 1.3→1.4**. 닭 HP는 2 유지(타격감·강화 브레이크포인트 보존) — 난이도는 압박과 도주 여유를 깎는 쪽으로 올림. 결과: 방치 사망 6→5초, 압박비 1.58→1.97, 만재 도주 여유 20%→11%. ⚠피격무적은 07-10 확정값이라 수치만 조정한 것 기획 확인 대기. 팝업은 **강화 팝업·맵 해금 팝업 2종**을 텍스트 와이어프레임으로 규격화(공통: 발판 진입 시 자동 열림·존 이탈 시 자동 닫힘·닫기버튼 없음) | `docs/BALANCE.md` v0.3, `docs/BIBLE.md` §10-b | 개발: BAL 4개 값 갱신 + 팝업 2종 구현 가능
- 2026-07-24 | 개발↔아트 | **맵 배경 렌더링 방식 변경**: 바닥은 타일 조합이 아니라 아트가 만든 통짜 배경 이미지로 진행. 타일은 오브젝트·트리거 등 필요한 것만 그 위에 얹는 용도로만 사용 | `docs/DECISIONS.md` | 개발: 배경 이미지 로드 구조로 전환 / 아트: 마을·던전 통짜 배경 이미지 제작
- 2026-07-24 | 기획→개발 | **HUD 최소화 원칙 확정** (화이트아웃 서바이벌·탕탕특공대 레퍼런스): 상시 코너 HUD 없음(골드·운반량 숫자 상시 표시 안 함). **HP바는 캐릭터 머리 위 플로팅(다이제틱), 사냥지대에서만 표시·마을에서는 숨김.** 운반량은 등짐 스택 자체로만 표현(별도 숫자 없음). 골드는 판매 시 "+N" 팝업으로만 등장, 누적 총액은 강화 팝업 안에서 확인 | `docs/BIBLE.md` §10-a | 개발: 기존 HUD 구현 있다면 이 원칙에 맞춰 축소 필요
- 2026-07-24 | 기획→개발 | **기획회의 결과 반영 — 마을·강화·몬스터 대개편.** ① **강화 3종만 배치**(공격력·이동속도·운반 — 무기손질대/신발정비대(임의)/운반구공방, 트리거+팝업에 현재스탯·보유골드·상승량·필요골드 표시). **진행(심층해금)은 오브젝트 아닌 맵 경계 게이트 트리거**로 별도 처리, 최초 마을→사냥지대 경계는 무료 튜토리얼. ② **몬스터를 닭·돼지·소 등 친숙한 동물로 전환**(C8 방향 확정, 3/5종 확정·2종 미정). ③ **비서 NPC는 후순위 보류**(공수 문제 — C10 보류 태그). ④ **조리·판매 플로우 확정**: 그릴 트리거(생고기→그릴)→조리→**그릴 수령 트리거(구운 고기, 플레이어가 직접 등에 재적재 — 운반이 핵심 재미이므로 자동 서빙 아님)**→판매 트리거(순차 판매)→**판매 즉시 골드 UI 자동 상승(별도 회수 없음)**. 손님은 타일당 2명 대기+**인내 게이지**(C11, 표현방식 미정). ⚠ **개발 리워크 요청**: 어제 구현된 `serving-counter`(그릴→자동서빙) 로직을 "그릴 수령"(플레이어 트리거)으로 교체 필요, `checkout`→`money-pickup` 2단계 회수 로직 제거하고 판매 트리거 시점에 즉시 `addGold` 호출로 단순화 필요. 손님 "타일당 2명"은 현재 1칸=1명 구조와 달라 방식 협의 필요(레인 분리 등). 코드에 이미 있던 임의 수치(이동0.3s·굽기1s·재스폰3s·판매당골드1)는 BALANCE.md에 그대로 동기화해둠 | `docs/BIBLE.md` §2·§7·§8·§9, `docs/DECISIONS.md`, `docs/BALANCE.md` | 개발: 위 리워크 2건 + 손님 배치 방식 확인 / 아트: 강화 오브젝트 3종·동물 몬스터 5종(3종 확정) 시안 착수 가능
- 2026-07-24 | 개발→전체 | **정육식당 생산 체인 + 손님 NPC 구현**: 맵 에디터에 **트리거**(triggers 루트) 6종 — 음식재료제공(ingredient-dropoff)→고기굽기(cooking)→판매대(serving-counter)→구매위치(purchase-spot), 정산대(checkout)→구매위치, 회수위치(money-pickup)→정산대. 트리거는 위치·크기(타일)·연결ID(triggerLink1/objectLink1)를 가지며 오브젝트와 겹칠 수 있음(트리거=로직, 연결 오브젝트=영역 표시 비주얼). 플레이어가 타일에 서 있거나 아이템이 쌓이면 타입별 처리(고기→요리→판매→정산→골드 회수, 아이템은 0.3s 포물선 이동·굽기 1s/개, 전부 임의값). **오브젝트 walkable 속성**(플레이어 통과 여부). **타일 속성 확장**: 대기열(attr=2)·퇴장(attr=3) — MapTile Inspector 드롭다운(없음/이동불가/대기열/퇴장). **손님 NPC**(kind=customer/손님): 대기열 타일 따라 한 칸씩 이동(앞 손님이 막으면 정지=줄서기, 4방향·대각선 제외·이미 밟은 칸 제외)→줄 끝에서 요리 대기→판매대가 요리 주면 만족→정산대가 돈 회수하면 퇴장 타일 따라 이동→끝나면 비활성·재스폰 | `game/assets/scripts/ingame/{TriggerSystem,CustomerSystem}.ts`·`mapedit/{MapTrigger,MapObject,MapTile,MapUnit}.ts`·`IngameBootstrap.ts`·`MapData.ts` | 디자이너: 마을에 대기열/퇴장 타일 칠하고 트리거 6종 배치·연결(판매대·정산대는 구매위치 타일과 겹치게), 손님 NPC(kind=customer)를 대기열 시작 칸에 배치 후 재내보내기 / 기획: 아이템 이동속도(0.3s)·굽기속도(1s)·재스폰 주기(3s)·회수 골드액 정의 필요 (전부 임의)
- 2026-07-19 | 개발→전체 | **맵 에디터 배치물 편집 추가**: MapRoot 아래 종류별 루트(objects/monsters/npcs/spawn)에서 오브젝트·몬스터·NPC·플레이어를 편집. 각 노드 Inspector에서 **img(외형)·kind(종류)** 지정 — 오브젝트는 **tileW/H(타일 단위 크기)**로 타일 위에 스냅. MapEditRoot 체크박스(addObject/addMonster/addNpc)로 추가, export/import 왕복 보존. 외형 이미지 규칙: `resources/maps/objs·units/{ID}_{이름}.png` (예제 오브젝트 8종·유닛 6종 포함). **게임 연동**: 오브젝트·NPC 렌더, spawn 마커 img=플레이어 외형 교체, **몬스터 배치의 kind가 그 던전의 스폰 종류가 됨** (에디터 배치 > 코드 표 DUNGEON_KINDS > 기본 slime) | `game/assets/scripts/{mapedit,ingame}/` + `resources/maps/objs·units/` | 디자이너: 에디터에서 배치 후 재내보내기 / 기획: 몬스터 kind 이름은 CombatSystem 종류 키와 일치해야 함(현재 slime만 구현 — 다른 kind 배치 시 슬라임 외형·스탯으로 동작)
- 2026-07-10 | 기획→전체 | **비서 NPC 상호작용 방식 확정: 퀘스트보드.** 비서 존 진입 시 퀘스트보드 UI(마을 문법 그대로) — 퀘스트 수행 완료 시 보상+친밀도 상승. 대화식 호감도 이벤트는 채택 안 함. 세부(퀘스트 종류·빈도·보상)는 C10 계속 미결 | `docs/BIBLE.md` §9-b, `docs/DECISIONS.md` | 개발: 퀘스트보드 UI(존 진입 트리거) 구조 설계 가능
- 2026-07-10 | 기획→전체 | **비서 NPC 서사·엔딩 확정**: 엘프족/수인족 등 이세계풍 캐릭터, 주인공은 이송된 금사빠 찐따. 비서=돌발퀘스트·VIP오더로 던전 심층 유도하는 동기부여 열쇠. 친밀도 보상 방향: 무기 아이템 지급 + 호객으로 판매속도 상승. **궁극 엔딩: 친밀도 100%=결혼.** 상호작용 방식·수치는 C10 계속 미결 | `docs/BIBLE.md` §6·§9-b, `docs/DECISIONS.md` | 아트: 비서 캐릭터(엘프/수인) 시안 방향 참고 / 기획: C10(친밀도 상호작용 방식) 결론 필요
- 2026-07-10 | 기획→전체 | **강화 오브젝트 4종 확정(콘셉트)** + **비서 NPC 신규 기획**: 강화는 사냥/운반/상점/진행 각 1개(무기손질대·운반구공방·호객안내대·봉인제단, 책상 크기). 비서 NPC는 마을 고정 배치, 돌발 알림은 우측 상단 얼굴+말풍선, 친밀도가 실플레이 효과로 연결됨(상호작용 방식·효과·퀘스트 내용은 미정 — **C10 등록**). C9(오브젝트 시각변화)도 계속 미결 | `docs/BIBLE.md` §7-b·§9-b, `docs/DECISIONS.md` | 아트: 4개 오브젝트 + 비서 캐릭터 시안 착수 가능 / 개발: 비서 알림 UI(우측상단 팝업) 구조 설계 가능 / 기획: C9·C10 결론 필요
- 2026-07-10 | 기획→개발 | **마을 상호작용 가결정 2건**: ① **판매** — 판매존 진입 시 등짐 고기 한 조각씩 판매대로 순차 이송, 손님이 사가며 골드 서서히 상승, 존 이탈 시 이송 정지(안 넘어간 고기는 등짐에 남음, 재진입 시 이어짐) ② **강화** — 강화존 진입 시 즉시 팝업(스탯 예시+가격), 1회/10회 구매 옵션. "월드-인 게이지(35/100)" 설계 폐기. **미결 C9**: 강화 오브젝트 시각 변화 여부 | `docs/BIBLE.md` §7-a·§9-a, `docs/DECISIONS.md` | 개발: 마을 서브존 구조(판매존 1개 + 강화존 N개) 설계 시작 가능 / 아트: C9 논의
- 2026-07-10 | 개발→전체 | **지역 규칙 변경 (구 d{ID} 네이밍 대체)**: ① 지역 **이름은 자유 라벨** (예: "슬라임 굴", "시작 마을" — 게임 배너에 그대로 표시) ② **모든 지역(마을·통로 포함)이 고유 ID 필수** — 생성 시 자동 부여, 내보내기 시 중복/미지정 경고. 몬스터 스폰 설정(DUNGEON_KINDS)은 던전 지역의 ID를 키로 사용(변동 없음). 아래 "던전 ID 명시 지정" 항목의 d{ID} 네이밍 규칙 문구는 폐기 | `TileRegion.ts`·`MapEditRoot.ts` | 기획: 몬스터 표는 지역 ID 기준으로 (이름은 참고용 라벨) / 디자이너: 지역 이름 자유롭게 짓고 ID만 고유하게
- 2026-07-10 | 개발→전체 | **맵데이터에 지역 정보 저장**: 구역 이름·던전ID·기하를 mapdata.json `regions`에 포함 — 임포트 시 구역이 이름·ID까지 그대로 복원(왕복 완전 보존), 게임은 던전 진입 배너에 지역 이름(d1 등) 표시(던전 간 직접 이동도 배너 재표시). **방침: 맵 에디터 기능 완성 후 mapdata 저장 포맷을 JSON → 바이너리로 전환** (용량·로드 속도 — 전환 시점에 포맷 설계) | `MapData.ts`·`MapEditRoot.ts`·`IngameBootstrap.ts` | 디자이너: 재내보내기 1회 (regions 포함시키기)
- 2026-07-10 | 개발→전체 | **던전 ID 명시 지정 방식 확정**: 구역(TileRegion)의 Inspector에 **regionId** 노출 — 내보내기 시 그 구역의 던전 타일에 ID로 기록되고, 게임의 몬스터 소속·스폰 설정이 이 값을 사용. 자동 번호(그리기 순서에 따라 밀리는 문제)를 대체하는 안정적 방식. 구역 이름 규칙 = **d{ID}** (d1, d2…). ID 미지정 던전은 자동 부여(1000+) | `TileRegion.ts`·`MapEditRoot.ts`·`MapData.ts` | **기획: 던전 ID(d1, d2…)별 리스폰 몬스터 표를 이 ID 기준으로 작성** — `CombatSystem.ts DUNGEON_KINDS`에 반영됨 / 디자이너: 각 던전 구역에 regionId 고유 번호 지정 후 재내보내기
- 2026-07-10 | 개발→전체 | **맵 파이프라인 v2 안정화 완료 — WIP 해제** ✅: 맵에디트 씬 정상 동작(타일 이미지 표시·구역 이동=지역 이동·모서리 고정 성장·import/export 왕복·자동 백업). **던전 인스턴스 시스템** 신설: 이어진 던전 타일 덩어리마다 ID 자동 부여(d1, d2… — 에디터 구역 이름과 일치), 몬스터는 소속 던전 밖 이동 불가·다른 던전으로 플레이어 이동 시 복귀. **던전 ID별 스폰 몬스터 설정 구조** 준비됨(`CombatSystem.ts DUNGEON_KINDS` — 현재 slime뿐) | `game/assets/scripts/{ingame,mapedit}/` | **기획: 던전 ID(d1, d2…)별 리스폰 몬스터 종류·비율 표 정의 필요** (BALANCE에 추가 — 적 종류는 Phase 3 라인업(C8)과 연동) / 개발: 몬스터 종류별 구현은 기획 표 확정 후
- 2026-07-10 | 개발→전체 | **맵 파이프라인 v2 (⚠️WIP — 맵에디트 기능 아직 정상 동작 안 함, 이어서 작업 예정)**: 맵 편집을 Tiled → Cocos 씬(mapedit, 빌드 제외)으로 전환. 바닥=가상화 타일 렌더(화면 분량만 노드, 재사용), 존=타일 zone 속성(enum: 1마을·2던전·3통로), 이동불가=타일 attr 1(벽 폐기), 배치물=구역(region) 소속, 타일 이미지 규칙 `{ID}_{이름}.png`(테스트 32종 포함). 게임은 mapdata.json(v2)만 읽음 | `game/assets/scripts/{ingame,mapedit}/` + `resources/maps/` | 개발: 맵에디트 동작 안정화 → 팀 공유 / 기획: 통로(zone 3) 규칙·타일 attr 표 확정
- 2026-07-10 | 개발→전체 | **PHASE1 개발분 완료 — 코어 루프 전체 동작** 🎉: 웨이브(던전 존 내 스폰·마을 진입 금지·복귀 AI) → 단일타겟 자동공격(넉백·타격감) → 고기 드랍/픽업 → **등짐 스택★(관성 흔들림·한계 10·가득참 표시)** → HP·접촉피격·죽음(고기 손실, 마을 풀피 부활). BALANCE v0.2 전 항목 반영(동시 18·주기 2.0s·무게 -20px/s·마을 회복 +2/s). 캐릭터 원화(좌/우)·존별 바닥 타일·터치 조이스틱·안드로이드 빌드(UI버퍼 9004 픽스) 포함. 맵·벽은 Tiled 편집(가이드: game/assets/resources/maps/README.md — ⚠️외부 수정 후 Reload 필수) | `game/assets/scripts/ingame/` + `resources/maps·chars·tiles` | 전원: 플레이해서 **"스택이 재밌는가" 판정**(PHASE1 완료 기준 5) + 줌 1.5 최종 확정(우상단 ± 버튼) / 기획: 부활 위치·C7-b·바닥고기 소멸 논점
- 2026-07-10 | 기획→개발 | **Phase 1 임시 밸런스 v0.2** (전 항목 (임의), 절대값 방식) — 개발 임시값 대부분 수용, 조정 3곳: 동시최대 24→**18**·스폰주기 1.2→**2.0s**(유입이 처치의 2.6배라 존버 불가 → 하향), **무게 페널티 개당 -20px/s**(만재 300px/s). 신규: 마을 회복 +2/s·던전 회복 0, 드랍 60% 1~2개, 강화 1차 스케치. 피격 무적 0.6s = **캐릭터 기준(전체 공유) 확정** | `docs/BALANCE.md` | 개발: 반영 완료됨(위 항목) → 플레이 체감 피드백
- 2026-07-10 | 개발→전체 | **인게임 뷰 프로토타입 1차**: 아이소 다이아몬드 바닥(패턴 1노드, 대형맵 대응) + 이동/카메라 + 존 구분(허브/던전, 경계 판정) + 배치물 + 깊이정렬. 웹빌드 검증 완료. **확정값: 타일 128×64(2:1) · 기본 줌 1.5 · 해상도 1080×1920 세로**. 맵은 MapData 구조로 분리(추후 디자이너 Tiled 편집 대비) | `game/assets/scenes/ingame` + `game/assets/scripts/ingame/` | 이동속도는 무게/운반 기획 대기(프리뷰에서 Z/X 실험 가능) / 개발 다음: 웨이브·자동공격·고기 드랍 ※줌 1.5는 C7(줌인 스케일) 지시에 따라 재검증 예정
- 2026-07-10 | 기획→전체 | **Slack↔GitHub 연동 완료** — 이제 push마다 커밋 메시지가 슬랙에 자동 알림됨. 이 메시지가 보이면 연동 성공 🎉 | GitHub Slack 앱 | 팀원들도 각자 repo 초대 수락하기
- 2026-07-10 | 기획→전체 | **레벨·XP·3택1 완전 제거 확정** (성장=골드 영구 강화 일원화) + **C6 확정: 입장 재화 없음, 자유 출입.** PHASE1에서 XP/팝업 작업 삭제됨 — 개발 범위 축소 | `docs/` | 개발: PHASE1 개발 행 최신본 기준 (XP·팝업 없음)
- 2026-07-10 | 기획→전체 | **C7 구조 확정: A안 수시 왕복 — 핵심 재미 = 운반·리스크 테이킹.** 화면 스케일 = 화이트아웃 애드 수준 줌인 (30~100 물량 초안 폐기, 재산정). 3택1 역할은 C7-b로 계속 협의 | `docs/` | 개발: 줌인 스케일 기준으로 카메라·맵 비율 잡기 / 기획: 동시 몬스터 수 재산정
- 2026-07-10 | 기획(슬랙 스윕)→전체 | 브레인스토밍 정리: **결정 0건** (전부 아이디어 단계). IDEAS.md 신설(보스 히트 드랍·특수 부위·크리처류 등 8건), 협의 C7(런 구조&3택1 지속 ⭐)·C8(크리처 라인업) 등록 | `docs/IDEAS.md`, `docs/DECISIONS.md` | C7이 최우선 논점 — "성장의 재미: 정육점 vs 캐릭터" 축부터 정리
- 2026-07-09 | 정정 | 엔진 결정은 "Cocos 확정"까지만 — **기존 game/ 프로토타입 이어받기는 결정된 바 없음(개발 판단, 미정).** 아래 항목의 해당 문구는 무효 | `docs/DECISIONS.md` 정정됨 | —
- 2026-07-09 | 킥오프→전체 | **C1 확정: 이세계 정육식당** / **엔진 확정: Cocos** (game/ 프로토타입 이어받음) / 뷰는 2D 쿼터뷰 통일 | `docs/` | 아트: 이세계 정육식당 톤으로 시안 착수 / 개발: Cocos로 PHASE1 착수 / 남은 안건: C6 입장재화·C5 화면방향·C3 픽셀규격
- 2026-07-09 | 킥오프→전체 | **C4 확정: 씬 전환 없는 연속 맵** (아이소 다이아몬드, 마을=하단 코너, 경계 넘으면 사냥 지대). 뷰 전환 결정은 대체됨. BIBLE §4·PHASE1 §1~2 갱신 | `docs/` | 개발: 다이아몬드 맵 기준으로 착수 / 남은 킥오프 안건: C6 입장재화·C1 컨셉·C5 화면·엔진
- 2026-07-09 | 기획→전체 | **PHASE1 v1.0 확정 — 개발·아트 착수 가능.** 킥오프 안건 docs/KICKOFF.md 추가 (C4 던전구조·C6 입장재화 오늘 결정). 레벨업·택1(빈 팝업) Phase 1 포함 | `docs/` | 개발: PHASE1 개발 행 순서대로 착수 / 아트: 스타일 시안 착수(C1 논의 재료) / 기획: 킥오프 진행
- 2026-07-09 | 기획→전체 | **Phase 1 기획서 초안** 작성 (`docs/specs/PHASE1.md`, v0.9 컨펌 대기) — 허브(입구만 기능) + 던전 코어(스택 검증). 던전 구조 A/B/C는 미결로 명시, 임시 전투장으로 진행 | `docs/specs/` | 기획 컨펌 → v1.0 확정 → 개발/아트 착수
- 2026-07-08 | 기획→전체 | 기획 결정 4건 기록(목표 수준·플랫폼·조작·죽음 규칙), DECISIONS를 "협의 필요 항목만" 구조로 정리, ROADMAP.md(Phase 0~5) 신설 | `docs/` | 다음: C1(세계관)·C2(두 화면) 협의, 기획 파트장의 ROADMAP 검토
- 2026-07-08 | 개발→전체 | 이동 프로토타입 v0: 허브 바닥 + 정육점 주인 더미(WASD 이동, 카메라 추적). 상호작용 없음 | `game/` (Cocos 3.8.8 잠정) | 다음: BIBLE §12 순서대로 공격→웨이브→고기드랍. 엔진 결정(#9) 나면 유지/이관 판단
- 2026-07-08 | 셋업→전체 | 저장소 뼈대 + 요약 바이블 v0.1 작성 완료 | `docs/` | 팀원 합류 시 CLAUDE.md부터 읽기. DECISIONS #9(엔진)·#10(아트 방식)부터 결정 필요
