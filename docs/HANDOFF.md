# 파트 인계 로그 (Handoff)

> 파트를 토스할 때 **한 줄이라도** 남긴다. 최신이 위.
> 형식: `날짜 | 보낸파트→받는파트 | 무엇 | 어디에 | 다음 할 일`

---

- 2026-07-24 | 개발→전체 | **NPC 스폰 트리거 + 손님 NPC 대개편**: ① **스폰 트리거**(npc-spawn) 신설 — 조건 충족 시 손님 스폰(기획 규칙 정의 전 **10초에 1명**, 임의). 스폰마다 **spawnId 1씩 증가**해 부여(트리거 간 순서 판단용). 스폰 위치 두 슬롯이 다 차면 그 주기 건너뜀. 스폰 트리거 Inspector `npcImg`로 외형 지정. ② **손님은 이제 스폰 트리거로만 생성**(정적 배치 폐기) — CustomerSystem 풀 재사용. ③ **타일당 2명**(2:1 타일 좌/우 반씩, 슬롯 0/1). ④ **참을성/기분/성향**(전부 임의): 성향 급함·느긋함·시간무한 = 참을성 10·30·9999, 10초마다 1↓, 0이면 기분 1단계↓ 후 리셋. 기분 화남<지루함<보통<행복함, 머리 위 이모티콘(maps/moods 4종). 요리 받으면 행복. **화남에서 소진 시 즉시 포기 → 퇴장 타일로 나감**(퇴장 인접 없으면 사라짐 — 임의). ⑤ **구매위치**: 2명이면 **spawnId 작은(먼저 온) 손님 우선** 응대 | `game/assets/scripts/ingame/{TriggerSystem,CustomerSystem,IngameBootstrap}.ts`·`mapedit/{MapTrigger,MapEditRoot}.ts`·`resources/maps/moods/` | 디자이너: 대기열 시작에 **스폰 트리거** 배치(기존 customer NPC 정적 배치 대신), npcImg 지정 / 기획: 스폰 규칙·참을성/기분/성향 수치·포기 처리 정의 필요 (전부 임의)
- 2026-07-24 | 개발→디자인 | **오브젝트 리전 종속 + 이미지 크기 분리**: ① MapObject에 **regionId** — 값 입력 시 그 오브젝트 노드가 해당 리전의 `objects` 자식으로 편입되어 **리전과 함께 이동**(props와 동일 구조, 0=전역). 절대 위치 보존하며 자동 편입, export는 절대좌표로 변환·왕복 보존. ② **이미지 크기를 타일 크기(tileW/H)와 분리** — 발자국(점유)만 타일 단위, 외형 이미지는 `imgScale`(배율)·`imgOffX/Y`(px offset)로 독립 조절(앵커는 발자국 하단 꼭짓점 유지). 타일 크기를 바꿔도 이미지 크기는 안 변함 | `game/assets/scripts/mapedit/MapObject.ts`·`MapEditRoot.ts`·`ingame/{IngameBootstrap,MapData}.ts` | 디자이너: 오브젝트에 종속 리전 ID 지정하면 리전 이동 시 함께 움직임 / 이미지가 작으면 imgScale 키우기 / 기획: imgScale 기본 1(임의)
- 2026-07-24 | 개발→전체 | **바닥을 통짜 이미지로 전환 (타일별 이미지 폐지)**: 타일 디자인 시간이 너무 커서, 바닥을 타일마다 지정하지 않고 **구역(TileRegion) 하나에 큰 바닥 그림 1장**을 지정하는 방식으로 변경. TileRegion Inspector에 `floorImg`(resources/maps/floors/{ID}_{이름}.png)·`floorScale`(배율)·`floorOffX/Y`(px offset) 추가 — 에디터에서 크기·위치를 눈으로 맞추면 게임이 동일하게 렌더(같은 아이소 투영). **타일별 img 편집 제거**, 타일은 이제 **속성(attr)·존(zone)만** 편집(색으로만 표시). 게임은 바닥 이미지를 그리며 **화면 밖 이미지는 안 그림(컬링)**. 바닥 없는 곳은 TileView가 존 색 베이스만 깔아줌. 예제 바닥 3종(village/dungeon/grass, 1024×512 아이소 다이아몬드) | `game/assets/scripts/{ingame,mapedit}/`·`resources/maps/floors/` | 디자이너: 각 구역에 통짜 바닥 이미지 제작·지정(floorScale/off로 정렬) 후 재내보내기 / 참고: 기존 resources/maps/tiles(타일별 50종)는 이제 미사용 — 추후 정리 가능 / 기획: floorScale 기본값·offset은 임의(에디터에서 조절)
- 2026-07-24 | 기획→개발 | **밸런스 v0.3 — 첫 맵 난이도 +30%** + **팝업 UI 규격 문서화**. 밸런스 4곳만 변경(`CombatSystem.ts BAL` 갱신 필요): **스폰주기 2.0→1.6s** · **동시최대 18→22** · **피격무적 0.6→0.5s** · **닭 속도 1.3→1.4**. 닭 HP는 2 유지(타격감·강화 브레이크포인트 보존) — 난이도는 압박과 도주 여유를 깎는 쪽으로 올림. 결과: 방치 사망 6→5초, 압박비 1.58→1.97, 만재 도주 여유 20%→11%. ⚠피격무적은 07-10 확정값이라 수치만 조정한 것 기획 확인 대기. 팝업은 **강화 팝업·맵 해금 팝업 2종**을 텍스트 와이어프레임으로 규격화(공통: 발판 진입 시 자동 열림·존 이탈 시 자동 닫힘·닫기버튼 없음) | `docs/BALANCE.md` v0.3, `docs/BIBLE.md` §10-b | 개발: BAL 4개 값 갱신 + 팝업 2종 구현 가능
- 2026-07-24 | 개발↔아트 | **맵 배경 렌더링 방식 변경**: 바닥은 타일 조합이 아니라 아트가 만든 통짜 배경 이미지로 진행. 타일은 오브젝트·트리거 등 필요한 것만 그 위에 얹는 용도로만 사용 | `docs/DECISIONS.md` | 개발: 배경 이미지 로드 구조로 전환 / 아트: 마을·던전 통짜 배경 이미지 제작
- 2026-07-24 | 기획→개발 | **HUD 최소화 원칙 확정** (화이트아웃 서바이벌·탕탕특공대 레퍼런스): 상시 코너 HUD 없음(골드·운반량 숫자 상시 표시 안 함). **HP바는 캐릭터 머리 위 플로팅(다이제틱), 사냥지대에서만 표시·마을에서는 숨김.** 운반량은 등짐 스택 자체로만 표현(별도 숫자 없음). 골드는 판매 시 "+N" 팝업으로만 등장, 누적 총액은 강화 팝업 안에서 확인 | `docs/BIBLE.md` §10-a | 개발: 기존 HUD 구현 있다면 이 원칙에 맞춰 축소 필요
- 2026-07-24 | 기획→개발 | **기획회의 결과 반영 — 마을·강화·몬스터 대개편.** ① **강화 3종만 배치**(공격력·이동속도·운반 — 무기손질대/신발정비대(임의)/운반구공방, 트리거+팝업에 현재스탯·보유골드·상승량·필요골드 표시). **진행(심층해금)은 오브젝트 아닌 맵 경계 게이트 트리거**로 별도 처리, 최초 마을→사냥지대 경계는 무료 튜토리얼. ② **몬스터를 닭·돼지·소 등 친숙한 동물로 전환**(C8 방향 확정, 3/5종 확정·2종 미정). ③ **비서 NPC는 후순위 보류**(공수 문제 — C10 보류 태그). ④ **조리·판매 플로우 확정**: 그릴 트리거(생고기→그릴)→조리→**그릴 수령 트리거(구운 고기, 플레이어가 직접 등에 재적재 — 운반이 핵심 재미이므로 자동 서빙 아님)**→판매 트리거(순차 판매)→**판매 즉시 골드 UI 자동 상승(별도 회수 없음)**. 손님은 타일당 2명 대기+**인내 게이지**(C11, 표현방식 미정). ⚠ **개발 리워크 요청**: 어제 구현된 `serving-counter`(그릴→자동서빙) 로직을 "그릴 수령"(플레이어 트리거)으로 교체 필요, `checkout`→`money-pickup` 2단계 회수 로직 제거하고 판매 트리거 시점에 즉시 `addGold` 호출로 단순화 필요. 손님 "타일당 2명"은 현재 1칸=1명 구조와 달라 방식 협의 필요(레인 분리 등). 코드에 이미 있던 임의 수치(이동0.3s·굽기1s·재스폰3s·판매당골드1)는 BALANCE.md에 그대로 동기화해둠 | `docs/BIBLE.md` §2·§7·§8·§9, `docs/DECISIONS.md`, `docs/BALANCE.md` | 개발: 위 리워크 2건 + 손님 배치 방식 확인 / 아트: 강화 오브젝트 3종·동물 몬스터 5종(3종 확정) 시안 착수 가능
- 2026-07-24 | 개발→전체 | **정육식당 생산 체인 + 손님 NPC 구현**: 맵 에디터에 **트리거**(triggers 루트) 6종 — 음식재료제공(ingredient-dropoff)→고기굽기(cooking)→판매대(serving-counter)→구매위치(purchase-spot), 정산대(checkout)→구매위치, 회수위치(money-pickup)→정산대. 트리거는 위치·크기(타일)·연결ID(triggerLink1/objectLink1)를 가지며 오브젝트와 겹칠 수 있음(트리거=로직, 연결 오브젝트=영역 표시 비주얼). 플레이어가 타일에 서 있거나 아이템이 쌓이면 타입별 처리(고기→요리→판매→정산→골드 회수, 아이템은 0.3s 포물선 이동·굽기 1s/개, 전부 임의값). **오브젝트 walkable 속성**(플레이어 통과 여부). **타일 속성 확장**: 대기열(attr=2)·퇴장(attr=3) — MapTile Inspector 드롭다운(없음/이동불가/대기열/퇴장). **손님 NPC**(kind=customer/손님): 대기열 타일 따라 한 칸씩 이동(앞 손님이 막으면 정지=줄서기, 4방향·대각선 제외·이미 밟은 칸 제외)→줄 끝에서 요리 대기→판매대가 요리 주면 만족→정산대가 돈 회수하면 퇴장 타일 따라 이동→끝나면 비활성·재스폰 | `game/assets/scripts/ingame/{TriggerSystem,CustomerSystem}.ts`·`mapedit/{MapTrigger,MapObject,MapTile,MapUnit}.ts`·`IngameBootstrap.ts`·`MapData.ts` | 디자이너: 마을에 대기열/퇴장 타일 칠하고 트리거 6종 배치·연결(판매대·정산대는 구매위치 타일과 겹치게), 손님 NPC(kind=customer)를 대기열 시작 칸에 배치 후 재내보내기 / 기획: 아이템 이동속도(0.3s)·굽기속도(1s)·재스폰 주기(3s)·회수 골드액 정의 필요 (전부 임의)
- 2026-07-19 | 개발→전체 | **맵 에디터 배치물 편집 추가**: MapRoot 아래 종류별 루트(objects/monsters/npcs/spawn)에서 오브젝트·몬스터·NPC·플레이어를 편집. 각 노드 Inspector에서 **img(외형)·kind(종류)** 지정 — 오브젝트는 **tileW/H(타일 단위 크기)**로 타일 위에 스냅. MapEditRoot 체크박스(addObject/addMonster/addNpc)로 추가, export/import 왕복 보존. 외형 이미지 규칙: `resources/maps/objs·units/{ID}_{이름}.png` (예제 오브젝트 8종·유닛 6종 포함). **게임 연동**: 오브젝트·NPC 렌더, spawn 마커 img=플레이어 외형 교체, **몬스터 배치의 kind가 그 던전의 스폰 종류가 됨** (에디터 배치 > 코드 표 DUNGEON_KINDS > 기본 slime) | `game/assets/scripts/{mapedit,ingame}/` + `resources/maps/objs·units/` | 디자이너: 에디터에서 배치 후 재내보내기 / 기획: 몬스터 kind 이름은 CombatSystem 종류 키와 일치해야 함(현재 slime만 구현 — 다른 kind 배치 시 슬라임 외형·스탯으로 동작)
- 2026-07-10 | 기획→전체 | **비서 NPC 상호작용 방식 확정: 퀘스트보드.** 비서 존 진입 시 퀘스트보드 UI(마을 문법 그대로) — 퀘스트 수행 완료 시 보상+친밀도 상승. 대화식 호감도 이벤트는 채택 안 함. 세부(퀘스트 종류·빈도·보상)는 C10 계속 미결 | `docs/BIBLE.md` §9-b, `docs/DECISIONS.md` | 개발: 퀘스트보드 UI(존 진입 트리거) 구조 설계 가능
- 2026-07-10 | 기획→전체 | **비서 NPC 서사·엔딩 확정**: 엘프족/수인족 등 이세계풍 캐릭터, 주인공은 이송된 금사빠 찐따. 비서=돌발퀘스트·VIP오더로 던전 심층 유도하는 동기부여 열쇠. 친밀도 보상 방향: 무기 아이템 지급 + 호객으로 판매속도 상승. **궁극 엔딩: 친밀도 100%=결혼.** 상호작용 방식·수치는 C10 계속 미결 | `docs/BIBLE.md` §6·§9-b, `docs/DECISIONS.md` | 아트: 비서 캐릭터(엘프/수인) 시안 방향 참고 / 기획: C10(친밀도 상호작용 방식) 결론 필요
- 2026-07-10 | 기획→전체 | **강화 오브젝트 4종 확정(콘셉트)** + **비서 NPC 신규 기획**: 강화는 사냥/운반/상점/진행 각 1개(무기손질대·운반구공방·호객안내대·봉인제단, 책상 크기). 비서 NPC는 마을 고정 배치, 돌발 알림은 우측 상단 얼굴+말풍선, 친밀도가 실플레이 효과로 연결됨(상호작용 방식·효과·퀘스트 내용은 미정 — **C10 등록**). C9(오브젝트 시각변화)도 계속 미결 | `docs/BIBLE.md` §7-b·§9-b, `docs/DECISIONS.md` | 아트: 4개 오브젝트 + 비서 캐릭터 시안 착수 가능 / 개발: 비서 알림 UI(우측상단 팝업) 구조 설계 가능 / 기획: C9·C10 결론 필요
- 2026-07-10 | 기획→개발 | **마을 상호작용 가결정 2건**: ① **판매** — 판매존 진입 시 등짐 고기 한 조각씩 판매대로 순차 이송, 손님이 사가며 골드 서서히 상승, 존 이탈 시 이송 정지(안 넘어간 고기는 등짐에 남음, 재진입 시 이어짐) ② **강화** — 강화존 진입 시 즉시 팝업(스탯 예시+가격), 1회/10회 구매 옵션. "월드-인 게이지(35/100)" 설계 폐기. **미결 C9**: 강화 오브젝트 시각 변화 여부 | `docs/BIBLE.md` §7-a·§9-a, `docs/DECISIONS.md` | 개발: 마을 서브존 구조(판매존 1개 + 강화존 N개) 설계 시작 가능 / 아트: C9 논의
- 2026-07-10 | 개발→전체 | **지역 규칙 변경 (구 d{ID} 네이밍 대체)**: ① 지역 **이름은 자유 라벨** (예: "슬라임 굴", "시작 마을" — 게임 배너에 그대로 표시) ② **모든 지역(마을·통로 포함)이 고유 ID 필수** — 생성 시 자동 부여, 내보내기 시 중복/미지정 경고. 몬스터 스폰 설정(DUNGEON_KINDS)은 던전 지역의 ID를 키로 사용(변동 없음). 아래 "던전 ID 명시 지정" 항목의 d{ID} 네이밍 규칙 문구는 폐기 | `TileRegion.ts`·`MapEditRoot.ts` | 기획: 몬스터 표는 지역 ID 기준으로 (이름은 참고용 라벨) / 디자이너: 지역 이름 자유롭게 짓고 ID만 고유하게
- 2026-07-10 | 개발→전체 | **맵데이터에 지역 정보 저장**: 구역 이름·던전ID·기하를 mapdata.json `regions`에 포함 — 임포트 시 구역이 이름·ID까지 그대로 복원(왕복 완전 보존), 게임은 던전 진입 배너에 지역 이름(d1 등) 표시(던전 간 직접 이동도 배너 재표시). **방침: 맵 에디터 기능 완성 후 mapdata 저장 포맷을 JSON → 바이너리로 전환** (용량·로드 속도 — 전환 시점에 포맷 설계) | `MapData.ts`·`MapEditRoot.ts`·`IngameBootstrap.ts` | 디자이너: 재내보내기 1회 (regions 포함시키기)
- 2026-07-10 | 개발→전체 | **던전 ID 명시 지정 방식 확정**: 구역(TileRegion)의 Inspector에 **regionId** 노출 — 내보내기 시 그 구역의 던전 타일에 ID로 기록되고, 게임의 몬스터 소속·스폰 설정이 이 값을 사용. 자동 번호(그리기 순서에 따라 밀리는 문제)를 대체하는 안정적 방식. 구역 이름 규칙 = **d{ID}** (d1, d2…). ID 미지정 던전은 자동 부여(1000+) | `TileRegion.ts`·`MapEditRoot.ts`·`MapData.ts` | **기획: 던전 ID(d1, d2…)별 리스폰 몬스터 표를 이 ID 기준으로 작성** — `CombatSystem.ts DUNGEON_KINDS`에 반영됨 / 디자이너: 각 던전 구역에 regionId 고유 번호 지정 후 재내보내기
- 2026-07-10 | 개발→전체 | **맵 파이프라인 v2 안정화 완료 — WIP 해제** ✅: 맵에디트 씬 정상 동작(타일 이미지 표시·구역 이동=지역 이동·모서리 고정 성장·import/export 왕복·자동 백업). **던전 인스턴스 시스템** 신설: 이어진 던전 타일 덩어리마다 ID 자동 부여(d1, d2… — 에디터 구역 이름과 일치), 몬스터는 소속 던전 밖 이동 불가·다른 던전으로 플레이어 이동 시 복귀. **던전 ID별 스폰 몬스터 설정 구조** 준비됨(`CombatSystem.ts DUNGEON_KINDS` — 현재 slime뿐) | `game/assets/scripts/{ingame,mapedit}/` | **기획: 던전 ID(d1, d2…)별 리스폰 몬스터 종류·비율 표 정의 필요** (BALANCE에 추가 — 적 종류는 Phase 3 라인업(C8)과 연동) / 개발: 몬스터 종류별 구현은 기획 표 확정 후
- 2026-07-10 | 개발→전체 | **맵 파이프라인 v2 (⚠️WIP — 맵에디트 기능 아직 정상 동작 안 함, 이어서 작업 예정)**: 맵 편집을 Tiled → Cocos 씬(mapedit, 빌드 제외)으로 전환. 바닥=가상화 타일 렌더(화면 분량만 노드, 재사용), 존=타일 zone 속성(enum: 1마을·2던전·3통로), 이동불가=타일 attr 1(벽 폐기), 배치물=구역(region) 소속, 타일 이미지 규칙 `{ID}_{이름}.png`(테스트 32종 포함). 게임은 mapdata.json(v2)만 읽음 | `game/assets/scripts/{ingame,mapedit}/` + `resources/maps/` | 개발: 맵에디트 동작 안정화 → 팀 공유 / 기획: 통로(zone 3) 규칙·타일 attr 표 확정
- 2026-07-10 | 개발→전체 | **PHASE1 개발분 완료 — 코어 루프 전체 동작** 🎉: 웨이브(던전 존 내 스폰·마을 진입 금지·복귀 AI) → 단일타겟 자동공격(넉백·타격감) → 고기 드랍/픽업 → **등짐 스택★(관성 흔들림·한계 10·가득참 표시)** → HP·접촉피격·죽음(고기 손실, 마을 풀피 부활). BALANCE v0.2 전 항목 반영(동시 18·주기 2.0s·무게 -20px/s·마을 회복 +2/s). 캐릭터 원화(좌/우)·존별 바닥 타일·터치 조이스틱·안드로이드 빌드(UI버퍼 9004 픽스) 포함. 맵·벽은 Tiled 편집(가이드: game/assets/resources/maps/README.md — ⚠️외부 수정 후 Reload 필수) | `game/assets/scripts/ingame/` + `resources/maps·chars·tiles` | 전원: 플레이해서 **"스택이 재밌는가" 판정**(PHASE1 완료 기준 5) + 줌 1.5 최종 확정(우상단 ± 버튼) / 기획: 부활 위치·C7-b·바닥고기 소멸 논점
- 2026-07-10 | 기획→개발 | **Phase 1 임시 밸런스 v0.2** (전 항목 (임의), 절대값 방식) — 개발 임시값 대부분 수용, 조정 3곳: 동시최대 24→**18**·스폰주기 1.2→**2.0s**(유입이 처치의 2.6배라 존버 불가 → 하향), **무게 페널티 개당 -20px/s**(만재 300px/s). 신규: 마을 회복 +2/s·던전 회복 0, 드랍 60% 1~2개, 강화 1차 스케치. 피격 무적 0.6s = **캐릭터 기준(전체 공유) 확정** | `docs/BALANCE.md` | 개발: 반영 완료됨(위 항목) → 플레이 체감 피드백
- 2026-07-10 | 개발→전체 | **인게임 뷰 프로토타입 1차**: 아이소 다이아몬드 바닥(패턴 1노드, 대형맵 대응) + 이동/카메라 + 존 구분(허브/던전, 경계 판정) + 배치물 + 깊이정렬. 웹빌드 검증 완료. **확정값: 타일 128×64(2:1) · 기본 줌 1.5 · 해상도 1080×1920 세로**. 맵은 MapData 구조로 분리(추후 디자이너 Tiled 편집 대비) | `game/assets/scenes/ingame` + `game/assets/scripts/ingame/` | 이동속도는 무게/운반 기획 대기(프리뷰에서 Z/X 실험 가능) / 개발 다음: 웨이브·자동공격·고기 드랍 ※줌 1.5는 C7(줌인 스케일) 지시에 따라 재검증 예정
- 2026-07-10 | 기획→전체 | **Slack↔GitHub 연동 완료** — 이제 push마다 커밋 메시지가 슬랙에 자동 알림됨. 이 메시지가 보이면 연동 성공 🎉 | GitHub Slack 앱 | 팀원들도 각자 repo 초대 수락하기
- 2026-07-10 | 기획→전체 | **레벨·XP·3택1 완전 제거 확정** (성장=골드 영구 강화 일원화) + **C6 확정: 입장 재화 없음, 자유 출입.** PHASE1에서 XP/팝업 작업 삭제됨 — 개발 범위 축소 | `docs/` | 개발: PHASE1 개발 행 최신본 기준 (XP·팝업 없음)
- 2026-07-10 | 기획→전체 | **C7 구조 확정: A안 수시 왕복 — 핵심 재미 = 운반·리스크 테이킹.** 화면 스케일 = 화이트아웃 애드 수준 줌인 (30~100 물량 초안 폐기, 재산정). 3택1 역할은 C7-b로 계속 협의 | `docs/` | 개발: 줌인 스케일 기준으로 카메라·맵 비율 잡기 / 기획: 동시 몬스터 수 재산정
- 2026-07-10 | 기획(슬랙 스윕)→전체 | 브레인스토밍 정리: **결정 0건** (전부 아이디어 단계). IDEAS.md 신설(보스 히트 드랍·특수 부위·크리처류 등 8건), 협의 C7(런 구조&3택1 지속 ⭐)·C8(크리처 라인업) 등록 | `docs/IDEAS.md`, `docs/DECISIONS.md` | C7이 최우선 논점 — "성장의 재미: 정육점 vs 캐릭터" 축부터 정리
- 2026-07-09 | 정정 | 엔진 결정은 "Cocos 확정"까지만 — **기존 game/ 프로토타입 이어받기는 결정된 바 없음(개발 판단, 미정).** 아래 항목의 해당 문구는 무효 | `docs/DECISIONS.md` 정정됨 | —
- 2026-07-09 | 킥오프→전체 | **C1 확정: 이세계 정육식당** / **엔진 확정: Cocos** (game/ 프로토타입 이어받음) / 뷰는 2D 쿼터뷰 통일 | `docs/` | 아트: 이세계 정육식당 톤으로 시안 착수 / 개발: Cocos로 PHASE1 착수 / 남은 안건: C6 입장재화·C5 화면방향·C3 픽셀규격
- 2026-07-09 | 킥오프→전체 | **C4 확정: 씬 전환 없는 연속 맵** (아이소 다이아몬드, 마을=하단 코너, 경계 넘으면 사냥 지대). 뷰 전환 결정은 대체됨. BIBLE §4·PHASE1 §1~2 갱신 | `docs/` | 개발: 다이아몬드 맵 기준으로 착수 / 남은 킥오프 안건: C6 입장재화·C1 컨셉·C5 화면·엔진
- 2026-07-09 | 기획→전체 | **PHASE1 v1.0 확정 — 개발·아트 착수 가능.** 킥오프 안건 docs/KICKOFF.md 추가 (C4 던전구조·C6 입장재화 오늘 결정). 레벨업·택1(빈 팝업) Phase 1 포함 | `docs/` | 개발: PHASE1 개발 행 순서대로 착수 / 아트: 스타일 시안 착수(C1 논의 재료) / 기획: 킥오프 진행
- 2026-07-09 | 기획→전체 | **Phase 1 기획서 초안** 작성 (`docs/specs/PHASE1.md`, v0.9 컨펌 대기) — 허브(입구만 기능) + 던전 코어(스택 검증). 던전 구조 A/B/C는 미결로 명시, 임시 전투장으로 진행 | `docs/specs/` | 기획 컨펌 → v1.0 확정 → 개발/아트 착수
- 2026-07-08 | 기획→전체 | 기획 결정 4건 기록(목표 수준·플랫폼·조작·죽음 규칙), DECISIONS를 "협의 필요 항목만" 구조로 정리, ROADMAP.md(Phase 0~5) 신설 | `docs/` | 다음: C1(세계관)·C2(두 화면) 협의, 기획 파트장의 ROADMAP 검토
- 2026-07-08 | 개발→전체 | 이동 프로토타입 v0: 허브 바닥 + 정육점 주인 더미(WASD 이동, 카메라 추적). 상호작용 없음 | `game/` (Cocos 3.8.8 잠정) | 다음: BIBLE §12 순서대로 공격→웨이브→고기드랍. 엔진 결정(#9) 나면 유지/이관 판단
- 2026-07-08 | 셋업→전체 | 저장소 뼈대 + 요약 바이블 v0.1 작성 완료 | `docs/` | 팀원 합류 시 CLAUDE.md부터 읽기. DECISIONS #9(엔진)·#10(아트 방식)부터 결정 필요
